import { sql } from "drizzle-orm";
import { boolean, doublePrecision, index, integer, pgTable, serial, text } from "drizzle-orm/pg-core";

export const users=pgTable("users",{id:serial("id").primaryKey(),email:text("email").notNull().unique(),name:text("name").notNull(),role:text("role",{enum:["admin","operator","provider"]}).notNull(),active:boolean("active").notNull().default(true),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const providers=pgTable("providers",{id:serial("id").primaryKey(),userId:integer("user_id").references(()=>users.id),businessName:text("business_name").notNull(),phone:text("phone").notNull(),vehicleType:text("vehicle_type").notNull(),status:text("status",{enum:["offline","available","busy"]}).notNull().default("offline"),latitude:doublePrecision("latitude"),longitude:doublePrecision("longitude"),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_providers_status").on(t.status)]);
export const services=pgTable("services",{id:serial("id").primaryKey(),code:text("code").notNull().unique(),type:text("type").notNull(),company:text("company").notNull(),origin:text("origin").notNull(),destination:text("destination").notNull().default("—"),distance:doublePrecision("distance").notNull().default(0),price:integer("price").notNull(),status:text("status",{enum:["Disponible","Asignado","En camino","Finalizado"]}).notNull().default("Disponible"),providerName:text("provider_name"),eta:integer("eta"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_services_status_created").on(t.status,t.createdAt)]);
export const accessUsers=pgTable("access_users",{id:serial("id").primaryKey(),phone:text("phone").notNull().unique(),displayName:text("display_name").notNull(),role:text("role",{enum:["admin","operator","provider"]}).notNull().default("provider"),active:boolean("active").notNull().default(true),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const sessions=pgTable("sessions",{id:serial("id").primaryKey(),userId:integer("user_id").notNull().references(()=>accessUsers.id),tokenHash:text("token_hash").notNull().unique(),expiresAt:text("expires_at").notNull(),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_sessions_user_expires").on(t.userId,t.expiresAt)]);
